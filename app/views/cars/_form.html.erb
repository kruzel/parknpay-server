<%= form_for([@car.user, @car]) do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%=  @car.user.email %>
  </div>
  <div class="field">
    <%= f.label :license_plate %><br />
    <%= f.text_field :license_plate %>
  </div>
  <div class="field">
    <%= f.label :car_description %><br />
    <%= f.text_field :car_description %>
  </div>
  <div class="field">
    <%= f.label :car_image %><br />
    <%= f.file_field  :car_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
